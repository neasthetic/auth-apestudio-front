export interface User {
  _id: string;
  discordId: string;
  username: string;
  discriminator: string;
  email: string;
  avatar: string;
  role: string;
  accessToken: string;
  refreshToken: string;
  createdAt: string;
  updatedAt: string;
}
